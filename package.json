{
  "name": "react-community-app",
  "version": "1.0.0",
  "description": "React 기반 커뮤니티 앱 - AWS 배포 최적화",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy:s3": "npm run build && aws s3 sync build/ s3://your-bucket-name --delete",
    "deploy:amplify": "npm run build"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-router-dom": "7.8.0",
    "react-scripts": "5.0.1",
    "lucide-react": "0.539.0",
    "amazon-cognito-identity-js": "^6.3.0"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}